#include<stdio.h>
#include<stdlib.h>


int main()
{
    // char ch;
    // printf("Enter a character to convert to upper case to lower case or vice versa: ");
    // scanf("%c", &ch);
    // ch = ch ^ 32;  //XOR operator
    // printf("%c", ch);

    int x= 0 , y = 0 , z;
    z = ~x + ~y;
    printf("%d",z);
    return 0;
}